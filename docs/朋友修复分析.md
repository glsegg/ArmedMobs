# 朋友修复分析：`EdDYON/tarkovscav` 与我们的树逐文件对照

- 分析对象：`D:\deepseek\ArmedMobs`（我们，modId `tarkovscav`，显示名 "Armed Mobs"）
- 对照对象：`D:\deepseek\ArmedMobs\_friendfix\tarkovscav-main\`（朋友发布的副本，https://github.com/EdDYON/tarkovscav ）
- 本次分析**只读**：`src/`、`mods/`、`build/` 与朋友的树都未改动；本文件的草稿/中间产物在 `tools/spike/work/friendfix/`。
- 未运行 gradle、未运行 `tools/spike/selftest.ps1`、未编译、未进游戏。所有结论均为静态证据（哈希、逐行 diff、README/闸门对照）。
- **并发写入提醒**：分析期间另一名 agent 正在本树的生成器部分写文件（实测 `src/main/resources/data/tarkovscav/structures/*.nbt` 于 `2026-09-26 00:29-00:30` 被重写、`worldgen/structure_set/*.json` 于 `23:55` 被重写）。因此**本轮对 `.nbt` 与 `structure_set/*.json` 的具体字节/行数不是最终值**；但这恰好**独立印证了**它们的差异属于"我们的城区/据点/废土生成工作"（正在被重新生成），分类结论不变。核心结论所依赖的文件在本轮内**未被改动**（`RenderStateGuard.java`/`GunInHandGeoLayer.java`/`ScavRenderer.java` mtime 停留在 `2026-09-23`，`Config.java` 停留在 `2026-09-25 22:04`；分析结束时已重新核对 README §5f/§5k/§5w 与全部被引用的 `DEFAULT_*` 常量仍然一致）。

---

## 1. 朋友的仓库是什么

**结论：它是我们项目在"更早时点"的一份快照，被整理成可发布/可本地跑诊断的形态，并且朋友在渲染/枪械那一小块改成了自己的一套实现。** 它不是"我们的树 + 修好之后就发布"，因为它的总体名字、特性集合都停在我们更早的状态。

映射与统计（逐文件 SHA256 实测）：

| 项 | 数量 | 说明 |
| --- | --- | --- |
| 朋友树文件总数 | 322 | `java/`→我们的 `src/main/java/`，`resources/`→`src/main/resources/`，`tools/`→`tools/` |
| 双方字节完全相同 | **262** | 包括冻结的 `geo/scav.geo.json`、`animations/scav.animation.json` |
| 双方都存在但不同 | **57**（任务书里写的是 50） | 见 §4 |
| 只存在于朋友树 | **3**（任务书里写的是 2） | `dev.bat`（1,100 B）、`tools/check-render-state.ps1`（1,293 B）、`tools/RenderStateGuardSmoke.java`（7,059 B） |

关键证据 —— **朋友的树整体是更早的状态**：

- `resources/META-INF/mods.toml` 第 8 行：theirs `displayName="武装暴徒 (Armed Thug)"`，ours `displayName="Armed Mobs"`。
- `resources/pack.mcmeta`：theirs `"塔科夫Scav resources"`，ours `"Armed Mobs resources"`。
- `gun/GunAiState.java` 第 4 行注释：theirs `"Tarkov Scav" behaviour set`，ours `"Armed Mobs" behaviour set`。
- `TarkovScav.java` 第 39 行：theirs `塔科夫Scav (Tarkov Scav) - mobs that fight with TaCZ firearms...`，ours `Armed Mobs - gun-armed mobs...`；ours 还注册了 `ModWorldgen`、`DoorBehavior`、`WastelandTravel`。
- **特性标记计数**（对我们已知"后续功能"的符号在两个树的 `*.java` 里做引用计数）：

| 符号 | 朋友树引用数 | 我们引用数 |
| --- | --- | --- |
| `AiProfile` | **0** | 61 |
| `SquadCoordinator` | **0** | 4 |
| `DoorBehavior` | **0** | 7 |
| `targetExposedNow` | **0** | 9 |
| `GrenadeBallistics` | **0** | 18 |
| `WastelandTravel` | **0** | 24 |
| `DEPLOYMENT_BEACON` | **0** | 4 |

  即：朋友的快照**早于**我们的 AI 分层/小队协同、门、暴露目标、手雷弹道、城区/据点/废土维度、改名。这解释了 `Config.java`（+52 KB）、`GunBrain.java`（+26 KB）、`ModCommands.java`（+6 KB）等大文件差异 —— 都是**我们的后续工作**。

- 它是一份"本地诊断版"工程配置：`gradle.properties` 里 theirs `mod_version=0.0.0-local`、`mod_authors=Original mod authors (local diagnostic build)`；`settings.gradle` theirs `rootProject.name = 'tarkovscav-local'`。
- 它被**发布整理**过：`README.md` 只有 **16 字节**（我们是 365,251 字节 / 3,375 行），`tools/` 里**只有**两个新文件（我们的 `tools/*.js`、`selftest.ps1` 等都不在里面），`libs/*.jar` 也没有（我们的 `.gitignore` 第 18-20 行写明"TaCZ 是用户自己的下载，绝不提交/打包"）。
- `java/.../client/` 下五个姿态/模型文件与 `gun/GunPose.java`：见 §2，**字节完全相同**。

---

## 2. 六个 client/model 文件的哈希核对（任务 1）

在映射后的路径上两侧分别算 SHA256：

| 文件 | 朋友 (B) | 我们 (B) | 判定 |
| --- | --- | --- | --- |
| `client/GunnerVillagerModel.java` | 12,751 | 12,751 | **字节相同** |
| `client/PoseWriters.java` | 15,277 | 15,277 | **字节相同** |
| `client/ClipPose.java` | 7,467 | 7,467 | **字节相同** |
| `client/ArmPose.java` | 2,983 | 2,983 | **字节相同** |
| `gun/GunPose.java` | 4,457 | 4,457 | **字节相同** |
| `client/ModelRenderTypes.java` | 3,853 | 3,855 | **文本相同，仅行尾差异** |

`ModelRenderTypes.java` 的"2 字节差"已定案：**不是内容差异，是我们文件里有 2 个多余的 CR 字节**。

- 朋友：`CR=0, LF=72`（纯 LF）。
- 我们：`CR=2, LF=72` —— 第 **25** 行与第 **34** 行是 CRLF，其余是 LF。
- 这两行内容两侧逐字相同：第 25 行 `* selftest asserts that this package never calls {@code RenderSystem} directly.</p>`，第 34 行 `*/`。
- 把两侧 CRLF→LF 归一后 `$ta -eq $tb` 为 **True**。

**含义（很重要）**：朋友没有改这六个文件里的任何一个。所以"枪的位置"不可能藏在 `GunnerVillagerModel` / `PoseWriters` / `ClipPose` / `ArmPose` / `GunPose` / `ModelRenderTypes` 里，枪械位置差异只可能落在 `GunInHandGeoLayer`（实际渲染层）和 `Config`（变换默认值）这两处，而渲染状态差异只可能落在 `RenderStateGuard` / 三个渲染器里。

---

## 3. 渲染/枪械差异表（任务 2）

标号约定：**theirs** = `_friendfix` 树，**ours** = 我们的树。逐条给出"他们有 X / 我们有 Y"。

### 3.1 `client/GunInHandGeoLayer.java`（theirs 511 行 / ours 556 行）

| # | 位置 | 他们有 | 我们有 | 谁更新 / 为什么 | 判定 |
| --- | --- | --- | --- | --- | --- |
| G1 | 手部坐标系（**枪位置的核心**） | `applyPalmItemFrame(PoseStack)`：**只有** `mulPose(XP,-90)` + `mulPose(YP,180)`；注释 L501-504 说"原版那 +（±1/16,.125,-.625）平移是从手臂 pivot 出发的，我们的 locator 已经在掌心了，再重复一次会把握把推离手" | `applyVanillaHandFrame(PoseStack, boolean leftHand)`：`mulPose(XP,-90)` + `mulPose(YP,180)` + **`translate(leftHand ? -1/16 : +1/16, 0.125F, -0.625F)`**（L542-554） | **我们的是当前版本**：README §5f L566-573 把"`normalisedHand` 做的两件事"写成 `cancel duplicate R` + `vanilla hand frame: mulPose(X,-90) → mulPose(Y,180) → translate(±1/16, 0.125, -0.625)`；而且我们自己的闸门 `tools/mount_matrix.js` **L243 断言** `applyVanillaHandFrame\(poseStack, offhand\)` 存在、**L247 断言**这个 frame 就是 `mulPose(X,-90) * mulPose(Y,180) * translate(±1/16, 0.125, -0.625)`。用户的 `z=-0.7` 就是**在这个 frame 里**量出来的（README §5f L585-588） | **不需要移植**：把 theirs 的"去掉平移"拿过来，等于在用户标定好的坐标系里再整体位移 `(1/16, 0.125, -0.625)` ≈ `(0.0625, 0.125, -0.625)` 格，**会回退用户自己的游戏内校准**。若用户觉得"枪现在偏了"才需要 A/B → 见 §6 `需要用户拍板` |
| G2 | `displayContext` 的兜底（`mirrored`） | L328 硬编码：任何"TaCZ 画不出来"的 context 都 mirror 成 `ItemDisplayContext.THIRD_PERSON_RIGHT_HAND` | L345-347 三元：`THIRD_PERSON_LEFT_HAND → THIRD_PERSON_RIGHT_HAND`，其余（`FIRST_PERSON_*`）→ **`FIRST_PERSON_RIGHT_HAND`**；另加 L357-364 的 `cantdraw:` WARN | 两边都是"THIRD_PERSON_RIGHT_HAND 是正解"这一共识（README §5b L456-468 的字节码表）；我们的三元在 `FIRST_PERSON_*` 分支上兜底到**同样画不出来的** context（TaCZ 对 `FIRST_PERSON_*` 是 `return`），所以 theirs 的硬编码**更宽容**。只有用户把 `gunMountDisplayContext` 设成第一人称才会走到，而 README §5b 明确写"不要设成 FIXED / THIRD_PERSON_LEFT_HAND" | **需要用户拍板**（可选 1 行修正，见 §6 P3）。日常默认路径下不影响 |
| G3 | 外来（TaCZ）绘制的护栏 | `renderStackForBone` 里两次绘制都只 `try { guard.restore(); } finally { popPose(); }`，注释 L420-421："including texture units ...; preserve shader stencil" | 两次绘制都在 `finally` 之后追加 `RenderStateGuard.rebindTexture(this.renderer.getTextureLocation(animatable));` + `RenderStateGuard.forceAlwaysPassStencil();`（L470-472、L507-509），注释 L454-464 写明"旧代码只把 stencil 扳回来，贴图绑定从没修，这就是这个护栏现在补上的洞（README 5w）" | **我们的是更新版本**：README §5w L1470 起把这件事列为"修法（三处）"，第 2 条就是"`GunInHandGeoLayer` 的两次外来绘制都改成 `snapshot → try → finally { restore(); rebindTexture(模型贴图); forceAlwaysPassStencil() }`"；`tools/selftest_texture_state.js` L139-144 断言 `rebindTexture` 出现 **2 次**、`snapshot("item draw...` 出现 **2 次** | **不需要移植** |
| G4 | 文件头/注释 | L53-54："the normalised hand frame keeps the animated palm pivot and uses TaCZ's third-person positioning group" | L53-63：`<h2>The transform is YSM's own, not a guess</h2>` + YSM 2.6.5 的实测常量清单 | 我们的文档更晚、更细（对应 README §5b） | 不需要移植 |
| G5 | `displayContext` 文档 | L295-297 简短版 | L304-314 含 `FIXED` 的 `translate(0.5,2,0.5)`、`scale(-1,-1,1)` 镜像与 1.2 倍 fixed scale 的完整字节码结论 | 同上 | 不需要移植 |

### 3.2 `client/RenderStateGuard.java`（theirs 155 行 / ours 232 行）

这是**唯一一处朋友版本在功能上比我们更全**的地方。

| # | 项 | 他们有 | 我们有 | 判定 |
| --- | --- | --- | --- | --- |
| R1 | 设计口号 | L13-17：`"Preserve the incoming stencil test instead of forcing it to always pass."` + 明确说"不隔离 framebuffer、不恢复别人清掉的 stencil 内容、不是 shader 兼容保证" | L98-110 `forceAlwaysPassStencil()`：`GL11.glEnable(GL_STENCIL_TEST)` + `GlStateManager._stencilFunc(GL_ALWAYS, 0, 0xFF)`，注释 L12-13 写明目标是"把 stencil 强制成全通过" | **我们是更新版本**（README §5k L818-828 就是这套：三个渲染器 `snapshot → forceAlwaysPassStencil() → finally restore()`；`selftest_texture_state.js` 第 1 节逐文件断言）。theirs 是另一套哲学 |
| R2 | **背面模板** | `StencilFace` record（L136-153）分别 capture/restore `GL_FRONT` 与 `GL_BACK`，读 `GL20.GL_STENCIL_BACK_FUNC/REF/VALUE_MASK/WRITEMASK/FAIL/PASS_DEPTH_FAIL/PASS_DEPTH_PASS`，写 `GL20.glStencilFuncSeparate/glStencilMaskSeparate/glStencilOpSeparate` | **没有**：只经 `GlStateManager._stencilFunc/_stencilMask/_stencilOp` 处理正面（L133-135），无任何背面恢复 | **我们的缺口** → 值得移植 |
| R3 | **`GL_STENCIL_CLEAR_VALUE`** | L41 capture `GL11.glGetInteger(GL11.GL_STENCIL_CLEAR_VALUE)`，L78 `GL11.glClearStencil(this.clearStencil)` | **没有** | **我们的缺口** → 值得移植 |
| R4 | **活动纹理单元 + 单元 0..2 绑定** | L19-25 `ENTITY_TEXTURE_UNITS = 3`（base/overlay/lightmap）+ `activeTexture` + `activeBinding` + `textureBindings[3]`；L46-58 capture；L95-100 逐个 `activateTexture` + `bindTexture` 再恢复活动单元；L106-110 `activateTexture` 同时走 cache 与 raw `GL13.glActiveTexture`；L112-119 `bindTexture` 只在 unit 0..11 时写 cache、永远写 raw | **没有**：只 capture 单个 `this.texture`（L72、L90），restore 时 `GlStateManager._bindTexture(this.texture)`（L169）+ `resyncTextureBinding()`（L185-194），不碰活动单元、不碰单元 0..2 的三张绑定 | **我们的缺口**（对 shader pack 的多单元状态尤其相关）→ 值得移植 |
| R5 | **`RenderSystem` 的 12 个 sampler** | L25 `shaderTextures[12] // RenderSystem's 1.20.1 sampler array`；L56-58 capture `RenderSystem.getShaderTexture(i)`；L92-94 `RenderSystem.setShaderTexture(i, v)` 全量恢复 | **没有**：只在 `rebindTexture` 里 `RenderSystem.setShaderTexture(0, texture)`（L203）恢复 0 号 | **我们的缺口** → 值得移植 |
| R6 | 我们的独有修复 | 无 | `resyncTextureBinding()`（L185-194：把 cache 里的 id 反写回 raw 绑定，注释引 README 5w）+ `rebindTexture(ResourceLocation)`（L202-205）+ `restore()` 里真的恢复贴图（L156-173，注释 L157-168 说明"以前 capture 了却只打日志"）+ `describe()` 输出更全（L208-224） | **我们的是更新版本**：`tools/selftest_texture_state.js` L129 断言 `resyncTextureBinding()` 存在、L139-141 断言 `rebindTexture` 出现两次 | 不需要移植 |
| R7 | 日志 | `describe()` 打 `activeUnit=%d ... shaderTex0=%d`（L127-134） | `describe()` 打 `valueMask/writeMask/op/ depthTest/depthWrite/blend/cull/tex`（L208-224），另有 `entryDescription()` | 我们要看的量不同；无需移植 |

**小结**：`RenderStateGuard` 的**主设计**我们更新（还有 README §5k/§5w 与闸门背书），但 theirs 里的 **R2/R3/R4/R5 是我们确实没有的恢复动作**，这四条是"无论作者是谁都成立的功能缺口"。它们纯属防御性 GL 状态还原，**不参与用户的枪位置标定**，移植风险低。

### 3.3 `client/ScavRenderer.java`（theirs 58 行 / ours 69 行）

| 位置 | 他们有 | 我们有 | 判定 |
| --- | --- | --- | --- |
| `render(...)` | 只 `applyLiveScale(); super.render(...)`（L50-55） | `applyLiveScale(); RenderStateGuard guard = RenderStateGuard.snapshot("scav " + entity.getId()); RenderStateGuard.forceAlwaysPassStencil(); try { super.render(...) } finally { guard.restore(); }`（L54-66，注释引 README 5k 的字节码证据） | **我们的是后续工作**（README §5k L825-826；`selftest_texture_state.js` 第 1 节逐文件断言 `snapshot`+`force`+`finally restore`）→ **不需要移植** |

同一模式还在另外两个渲染器上，且**都是我们更新的**（任务 5 会一并列出）：

- `client/GunnerPillagerGeoRenderer.java`：theirs 注释 `"the item layer owns its state guard"`、`render()` 直接调 `super`；ours 包成 `snapshot("gunner_pillager " + id)` → `forceAlwaysPassStencil()` → `try/finally restore()`。
- `client/GunnerVillagerRenderer.java`：theirs 注释 `"Restore shared state without overriding the stencil test belonging to the surrounding render pass."`；ours 注释 `"...its held-item layer draws a TaCZ gun, whose renderer leaves the stencil func at GL_EQUAL/0 (README 5k). Bracket the whole entity draw with RenderStateGuard, exactly like the two Bedrock renderers."` 并调用 `forceAlwaysPassStencil()`。

### 3.4 结论：朋友的"渲染/贴图修复"与我们的修复是同一问题的两套解法

- 他们：`RenderStateGuard` = 全量 snapshot/restore（**含**双面模板、clear 值、活动单元、3 张单元绑定、12 个 sampler），**不**强制 always-pass。
- 我们：`RenderStateGuard` = 强制 always-pass + **贴图 cache/raw 重同步**（README §5k/§5w），**不**恢复双面模板/clear 值/活动单元/12 sampler。
- 两边都在"外来绘制"处加了护栏；我们的额外做了"模型贴图重新绑定"，他们的额外做了"背面模板与多单元恢复"。

---

## 4. Config 默认值差异表（任务 3）

对照方式：直接读两侧 `Config.java` 的 `DEFAULT_*` 常量（`defineInRange`/`defineListAllowEmpty` 用的就是这些常量）。

**有差异的只有 6 个值，全部落在用户游戏内标定的那几把键上。**

| 键 | 他们的值 | 我们的值 | 出处 | 判定 |
| --- | --- | --- | --- | --- |
| `gunnerVillagerGunRotation` | `["-40","0","0"]` | **`["5","0","0"]`** | ours `Config.java` L502-520 注释："The shipped value is the user's own in-game calibration... factory value was -90 ... user asked for 50 degrees back → -40 ... He then tuned it further in his own instance and settled on `5`, which is the release baseline"；README §5j 同述 | **移植会回退用户自己的校准** → 不需要移植 |
| `gunnerVillagerIdleGunRotation` | `["-47","0","0"]` | **`["-2","0","0"]`** | ours L522-539：`ARMS_REST + 0 + 5 - 2 - 90 = -130`（水平），README §5j 的四档剪影表 | 同上 → 不需要移植 |
| `gunnerVillagerGunOffset` | `["0","0.06","0.0"]` | **`["0","0.06","-0.09"]`** | ours L485-490："The -0.12 forward slide is the **user's pick between two offered values** (option B...)"；L1818-1820 "This is a USER-CONFIRMED value: the gun sat too far forward, the user asked for it 'a bit further back'" | 同上 → 不需要移植 |
| `gunnerVillagerAimArmPitch` | `0.0` | **`-57.0`** | ours L450 常量 `-57.0D`；L1773-1775 注释："The aiming silhouette the user confirmed in game was an absolute -100 degrees, i.e. an offset of -57" | 同上 → 不需要移植 |
| `renderScale` | `0.7` | **`0.77`** | ours L386：`0.77D; // = 0.7 x 1.1, the user value (release baseline)`；README §5b L428 的历史 | 同上 → 不需要移植 |
| `gunMountRifleOffset` / `gunMountPistolOffset`（共同默认 `DEFAULT_MOUNT_OFFSET`） | `["0","0","0"]` | **`["0","0","-0.7"]`** | ours L379 `DEFAULT_MOUNT_OFFSET = List.of("0","0","-0.7")`；README §5f L585-588："The user then tuned `z=-0.7` in game and the rifle moved forward, which is the authoritative statement..."；L1663-1665 配置注释同述 | 同上 → 不需要移植。**注意**：theirs 的注释还特意写 `"Older configs may retain [0, 0, -0.7], a compensation for the previous frame."` —— 与我们的"用户测量基线"叙述相反（见 §7 未核对） |

**两侧完全相同的值**（已逐项核对，列出以免遗漏）：

`gunnerVillagerReloadGunRotation` `["0","0","0"]`、`gunnerVillagerHunkerGunRotation` `["0","0","0"]`、`gunnerVillagerIdleGunOffset` / `ReloadGunOffset` / `HunkerGunOffset` 均 `["0","0","0"]`、`gunnerVillagerGunScale` `1.0`、`gunnerVillagerHoldArmPitch` / `ReloadArmPitch` / `HunkerArmPitch` 均 `0.0`、`gunnerVillagerGunAnchor` `"arms"`、`hideGunWhenIdle` `false`、`headRestPitchDegrees` `20.0`、`GUN_ANCHOR_BONE` 默认写死 `"RightHandLocator"`、`DEFAULT_MOUNT_ROTATION` `["0","0","0"]`、`DEFAULT_MOUNT_SCALE` `1.0`、`DEFAULT_MOUNT_DISPLAY_CONTEXT` `"THIRD_PERSON_RIGHT_HAND"`、`modelLayering` `"upperLower"`、`logGlState` `false`、`villagerRenderScale` `1.0`。

`hireGunWhenIdle`：**两棵树都没有这个键**（只有 `hideGunWhenIdle`，两侧都是 `false`）。

> 结论一句话：**枪变换默认值上，theirs 没有任何一处是该移植的"修正"；6 处差异全部是我们这边的用户实机标定值，照搬会退回旧手感。**

---

## 5. 朋友的两个新工具（任务 4）

### 5.1 `tools/check-render-state.ps1`（1,293 B）

它做的事（全文 15 行，已读完）：

1. `$projectDir = Split-Path -Parent $PSScriptRoot`；`$javaBin` 取 `$env:JAVA_HOME\bin`，**否则硬编码** `C:\Users\25773\.jdks\ms-17.0.20.1\bin`（L3）。
2. 要求 `build\classpath\runClient_minecraftClasspath.txt` 存在，否则抛 `'Run .\dev.bat prepare first.'`（L5）。
3. 用 `javac -encoding UTF-8 -implicit:none` 编译 **`RenderStateGuardSmoke.java` + `java\com\gfl\tarkovscav\client\RenderStateGuard.java`**（注意 L11 用的是 `$projectDir\java\...`，即**他们的目录布局**，我们的对应路径是 `src/main/java/...`），输出到 `build\render-state-check`。
4. 用 `java -cp <checkOutput>;<minecraft+forge cp>` 跑 `RenderStateGuardSmoke`，非 0 退出即抛 `'Render-state regression check failed.'`。

**它验证的是"真 GL 往返"**，不是文本断言：`RenderStateGuardSmoke.java`（124 行，已读完）做的事是 —— 起一个**隐藏 GLFW 窗口**（`GLFW_VISIBLE=false`，GL 3.2，8 位 stencil，`RenderSystem.initRenderThread()`），对 `entryUnit ∈ {0,2,7,15}` × `stencilEnabled ∈ {false,true}` × `emptyBase ∈ {false,true}` 共 8 个场景，用**裸 GL 调用故意制造 cache 与驱动的不一致**，然后逐项 `glGetInteger` 断言：

- `GL_ACTIVE_TEXTURE` 回到入口单元（`snapshot preserves active unit`，L48；`active unit`，L69；`cached active unit`，L70）；
- 纹理单元 0/1/2 的 `GL_TEXTURE_BINDING_2D` 回到入口值（L71-74）；
- `entryUnit >= 3` 时"shader 拥有的单元"绑定也回来（L75-78）；
- **`RenderSystem.getShaderTexture(i) == 100+i`，i=0..11**（L79）；
- **正面与背面**模板 func/ref/valueMask/writeMask/op 全部回来（L80-89）；
- `GL_STENCIL_CLEAR_VALUE`（L90）、stencil/depth/blend/cull 开关（L91-95）；
- `GL_NO_ERROR`（L96）；并 `GlStateManager._deleteTexture` 清理（L97-98）。

### 5.2 与我们现有闸门的关系

我们已有的三个相关物：

- `tools/selftest_texture_state.js`（11,722 B，**已进 `tools/spike/selftest.ps1` 的 `$nodeGates`，键名 `selftest_texture_state`，第 128 行**）—— 它是**源码正则断言**：第 1 节逐渲染器断言 `snapshot`+`forceAlwaysPassStencil`+`finally guard.restore()`；第 2 节断言外来绘制后再次 always-pass；第 3-4 节断言"只有 `RenderStateGuard` 一处碰 GL 状态"；第 5 节断言 `logGlState` 存在；并**在 javap 与 TaCZ jar 都在时转发运行 `gl_state_audit.js`，否则 SKIP**（L18-19）。
- `tools/gl_state_audit.js`（4,809 B）—— 纯 `javap` 文本解码器（`const fs = require('fs')`，无任何 GL/GLFW 调用），回答"TaCZ 到底把 stencil 放回去没有"。
- `client/RenderStateGuard.java` —— 被断言的对象本身。

**重叠与空白**：我们覆盖的是"**代码结构 + TaCZ 字节码**"，覆盖不到"**驱动/GL cache 的真实往返**"。朋友的 smoke test 恰好补这一段：它是**唯一会真正建立 GL context 并读回状态**的检查，也正是它能一眼照出我们 R2/R3/R4/R5 四个缺口（它的断言逐条对应他们 guard 的字段，**对我们的 guard 会失败**：我们不恢复背面模板、clear 值、活动单元、12 个 sampler，而它不检查我们的 `forceAlwaysPassStencil`/`rebindTexture`/`resyncTextureBinding`）。

### 5.3 建议（不采纳，仅建议）

- **不要照抄这两个文件**：`check-render-state.ps1` 硬编码了朋友的 JDK 路径（`C:\Users\25773\.jdk...`）与他们的 `java/` 目录布局；`RenderStateGuardSmoke.java` 断言的是**他们 guard 的契约**，直接对我们的树跑会 red。
- **值得采纳的是这个"真 GL 冒烟"思路，且要按我们的风格落地**：在 `tools/spike/` 放一个 Java 冒烟类（与现有 `AssetTest.java` / `JarVerify.java` / `GunPoolTest.java` / `GrenadeBallisticsTest.java` 同级），在 `tools/spike/selftest.ps1` 里注册一个步骤，断言**我们 guard 自己的契约**（`snapshot`→`forceAlwaysPassStencil`→`restore` 往返 + `resyncTextureBinding`/`rebindTexture` 把 cache 与 raw 重新对齐），并参数化 `JAVA_HOME`（参照 `dev-server.ps1` 的做法），不要写死用户目录。若采纳 §6 的 P1，则同一冒烟类里再断言那 4 条新恢复。
- 本轮**不新增任何闸门**，也不修改 `selftest.ps1`（任务要求：只建议）。

---

## 6. "不是我们后续工作"的文件与发现（任务 5）

先把**已被我们已知后续功能解释掉**的差异归好类（逐条验证过内容，不是靠体积猜的）：

| 差异 | 我们的后续功能 | 证据（抽样） |
| --- | --- | --- |
| `Config.java` +52 KB、`GunBrain.java` +26 KB、`CombatTactics.java`、`AccuracyProfile.java`、`GunUser.java`、`SniperBehavior.java`、`command/ModCommands.java`（+102 行）、`command/ClientCommands.java` | AI 分层 / 小队协同 / 暴露目标 / 伤害 / 狙击 / 改名 | 特性标记表（theirs 全 0）；`GunUser.java` 新增 `targetExposedNow()` 且注释 `"README 5ab"`；`SniperBehavior.java` 新增 `AiProfile.partialCoverBonus` |
| `grenade/GrenadeEntity.java`（-5 行）、`grenade/GrenadeThrowGoal.java`（+91 行） | 手雷弹道 | ours `GrenadeEntity` L183-186 注释："'Where would it come down' lives in GrenadeBallistics and nowhere else (README 5v). The old flat projection ... disagreed with the real flight" |
| `TarkovScav.java`、`registry/ModItems.java`、`ModCreativeTabs.java` | 废土维度 / 部署信标 / 门 | ours 新增 `WastelandTravel` 注册注释（`/armedmobs dimension`）、`DEPLOYMENT_BEACON`；`DoorBehavior` 注册 |
| `registry/ModSounds.java`、`voice/MobVoice.java`、`assets/.../sounds.json`、`voice_levels.json`、`lang/en_us.json`、`lang/zh_cn.json` | 语音 / 手雷音效新增 | ours 新增 `GRENADE_3..5` 与 121 个 sound event；`MobVoice` 新增 `grenadeShoutCooldown` 独立计时器 |
| `worldgen/structure/*.json`、`worldgen/structure_set/*.json`、`tags/worldgen/structure/city.json`、`forge/biome_modifier/add_scavs.json`、`structures/*.nbt` | 城区 / 据点 / 废土维度 | `"biomes": "#minecraft:is_overworld"` → `"#tarkovscav:city_biomes"`；`"type": "minecraft:random_spread"` → `"tarkovscav:wasteland_spread"` + `dense_spacing/frequency`；`city.json` 新增 `tarkovscav:city_strongpoint`；刷新权重 12→5 / 6→2 等（README §7i） |
| `tags/entity_types/faction_village_hostile.json` | 阵营 | `#minecraft:undead` 展开为 `#minecraft:skeletons` + `zombie`/`husk`/`drowned`/… 显式列表 |
| `mods.toml`、`pack.mcmeta`、`GunAiState.java`、`TarkovScav.java` | 改名 Armed Mobs | `"Armed Mobs"` vs `"武装暴徒 (Armed Thug)"` / `"Tarkov Scav"` |
| `worldgen/CityDistrictAssembler.java` | 城区/出生点 | `"city.foundationDepth"` → `"spawn.foundationDepth"` |
| `registry/ModItems`/`ModCreativeTabs`/`ClientCommands`（`/armedmobs` 双根字面量） | 改名 | ours `ClientCommands` L61-68 注册 `tarkovscav` 与 `armedmobs` 两棵命令树 |

### 真正"不能用我们后续工作解释"的，只有下面这些

| 文件 | 差异 | 我的发现与判定 |
| --- | --- | --- |
| `entity/SniperPillagerEntity.java`、`entity/SniperVillagerEntity.java` | theirs 在 `createSniperAttributes()` 里用 `Config.SNIPER_FOLLOW_RANGE.getDefault()` 并**额外重写 `onAddedToWorld()`** 用 `.get()` 落值（注释：`"Attribute registration runs before Forge loads the common config."`）；ours **删掉了 `onAddedToWorld()`**，直接在属性构造里 `Config.SNIPER_FOLLOW_RANGE.get()` | **这是唯一一处与渲染/枪械无关、也不在任务给定功能清单里的实质差异。** 两侧 `ScavEntity` 都用硬编码 `48.0D`（且该文件文本完全相同），所以没有"团队风格"可直接判先后。theirs 是 Forge 常见的防御写法（配置未加载时 `getDefault()`，加载后在 `onAddedToWorld` 落真值）；ours 更简，但**若属性注册早于 common config 加载，`.get()` 会抛 `IllegalStateException`**。我们的 jar 是发布基线、假定实测正常 → **未核对谁更新**；若无实机问题则不需要改（见 §7） |
| `client/ModelRenderTypes.java` | 文本完全相同，只有我们这边第 25/34 行是 CRLF（+2 字节） | 行尾噪音，**不是修复**。可选：统一成全 LF |
| `entity/ScavEntity.java`（+3 B）、`gradlew.bat`（+92 B） | 文本完全相同，纯 CRLF/LF 差异 | 行尾噪音，**不是修复** |
| `gradle/wrapper/gradle-wrapper.properties` | theirs：`downloads.gradle.org` + **`distributionSha256Sum=a4b4158…dc612`** + `networkTimeout=60000`；ours：`services.gradle.org` + `validateDistributionUrl=true` + `networkTimeout=10000` | ours 是 **Gradle 8.8 官方 wrapper 模板的原始形态**（`validateDistributionUrl`/`10000` 都是模板自带）；theirs 是**手工加过 SHA 钉死**的版本。不是渲染修复。若关心供应链，可**可选**采纳"钉 SHA"这一条（见 §6 结语） |
| `build.gradle`、`gradle.properties`、`settings.gradle`、`.gitignore` | theirs 是"本地诊断版"：`mod_version=0.0.0-local`、`mod_authors=Original mod authors (local diagnostic build)`、`rootProject.name='tarkovscav-local'`、`sourceSets.main { java.setSrcDirs(['java']); resources.setSrcDirs(['resources']) }`（这正是他们仓库的目录布局成因）；ours 是发布工程（`archivesName = mod_jar_name` = `armedmobs-<version>`、`options.release.set(17)`、AT 声明、maven-publish 等） | **发布/本地化差异**。另有一处值得记下：ours `forge_version=47.3.0` / `minecraft_version_range=[1.20.1,1.21)`，theirs `forge_version=47.4.10` / `minecraft_version_range=[1.20.1]` —— **谁更新未核对**（见 §7） |
| `dev.bat`（**只在他们那边**） | 34 行批处理：`set JAVA_HOME=%USERPROFILE%\.jdks\ms-17.0.20.1`、`set GRADLE_USER_HOME=D:\DevCache\gradle`，把 `prepare/build/idea/server/runClient` 包一层 | 朋友的本地开发脚本（注释自称"sibling Forge projects"），与我们 `tools/spike/dev-server.ps1`、`run-server.cmd` 同类但环境路径是他的。**不是修复**；我们不需要它 |
| `README.md` | theirs **16 B**（一句话占位）vs ours 365,251 B | 发布时被剥掉，**不是修复** |
| `client/GunInHandGeoLayer.java` 的 G1/G2、`client/RenderStateGuard.java` 的 R2-R5 | 见 §3 | **归属无法证明**：无法判定"他们的去掉平移 / 155 行 guard"是朋友自己的改写，还是我们更早的历史状态（见 §7） |

**结论**：除 `SniperPillagerEntity`/`SniperVillagerEntity` 这一处外，**没有发现任何"藏着朋友的修复而我们没有"的第三个位置**；朋友真正改到渲染/枪械的地方就是 §3 那三个文件，而其中只有 R2/R3/R4/R5 是我们确实缺失的能力。

---

## 7. 收尾清单

### 值得移植（未应用，给出最小改动）

- **P1（推荐，低风险）—— 把 theirs 的四条防御性恢复补进我们的 guard。**
  文件：`src/main/java/com/gfl/tarkovscav/client/RenderStateGuard.java`；函数：构造器 `RenderStateGuard(String where)` + `restore()`。
  改动（**保留**我们现有的 `forceAlwaysPassStencil()` / `rebindTexture()` / `resyncTextureBinding()` 不动，只**增加**）：
  1. **双面模板**：新增一个 `StencilFace` 辅助 record，`capture(false)` 读 `GL11.GL_STENCIL_FUNC/_REF/_VALUE_MASK/_WRITEMASK/_FAIL/_PASS_DEPTH_FAIL/_PASS_DEPTH_PASS`，`capture(true)` 读对应的 `GL20.GL_STENCIL_BACK_*`；`restore()` 里在现有 `GlStateManager._stencilFunc/_stencilMask/_stencilOp`（正面 cache 写入）之后，对 `GL_FRONT`、`GL_BACK` 各调一次 `GL20.glStencilFuncSeparate/glStencilMaskSeparate/glStencilOpSeparate`（对应 theirs L136-153）。
  2. **clear 值**：构造器里 `this.clearStencil = GL11.glGetInteger(GL11.GL_STENCIL_CLEAR_VALUE);`，`restore()` 里 `GL11.glClearStencil(this.clearStencil);`（theirs L41、L78）。
  3. **活动单元 + 单元 0..2 绑定**：新增 `ENTITY_TEXTURE_UNITS = 3`、`activeTexture`、`activeBinding`、`textureBindings[3]`；构造器按 theirs L46-58 的方式 capture（注意其 `finally { activateTexture(this.activeTexture); }` 还原活动单元）；`restore()` 里按 theirs L95-100 逐个 `activateTexture(GL_TEXTURE0+i)` + `bindTexture(...)`，最后 `activateTexture(this.activeTexture); bindTexture(this.activeBinding);`。`activateTexture` 要**同时**写 `GlStateManager._activeTexture` 与裸 `GL13.glActiveTexture`；`bindTexture` 只在 unit 0..11 时写 cache、**永远**写裸 `GL11.glBindTexture`（theirs L106-119）。
  4. **12 个 sampler**：新增 `shaderTextures[12]`，构造器 `shaderTextures[i] = RenderSystem.getShaderTexture(i)`，`restore()` 里 `RenderSystem.setShaderTexture(i, shaderTextures[i])`（theirs L25、L56-58、L92-94）。
  配套（同一批做）：在 `tools/selftest_texture_state.js` 末尾按现有 `check(...)` 风格**追加**对这四条的断言（不要削弱任何现有断言）。
- **P2（推荐，但成本更高）—— 采纳"真 GL 冒烟"这个闸门思路，而不是他们的脚本。**
  新增 `tools/spike/RenderStateGuardSmoke.java`（我们的包名/风格），断言**我们**的契约：`snapshot` → `forceAlwaysPassStencil` → `restore` 后 `GL_ACTIVE_TEXTURE`/单元 0..2 绑定/`GL_STENCIL_FUNC..` 往返一致，以及 `resyncTextureBinding()` 后 `RenderSystem.getShaderTexture(0)` 与 `GL_TEXTURE_BINDING_2D` 一致；场景参数化（至少 `entryUnit ∈ {0,2,7,15}` × stencil on/off）。在 `tools/spike/selftest.ps1` 注册一步，`JAVA_HOME` 走环境变量/参数，**不要**硬编码 `C:\Users\25773\...`（他们脚本 L3 的写法）。若做了 P1，同处补断 R2-R5。
- **P3（可选，1 行，需先拍板）—— `displayContext` 的第一人称兜底。**
  文件：`client/GunInHandGeoLayer.java`，函数 `displayContext(boolean, ItemStack)`，L345-347。把 `else` 分支从 `ItemDisplayContext.FIRST_PERSON_RIGHT_HAND` 改成 `ItemDisplayContext.THIRD_PERSON_RIGHT_HAND`（或直接删掉三元、与 theirs L328 一样硬编码），这样即便用户把 `gunMountDisplayContext` 设成 `FIRST_PERSON_*`，枪也仍然可见（现在会兜底到同样"画不出来"的 context）。只在非默认配置下才会走到，属可选加固。

### 不需要移植

- theirs 的**手部坐标系** `applyPalmItemFrame`（去掉 `translate(±1/16, 0.125, -0.625)`）—— **会回退用户自己的游戏内校准**（README §5f、我们的闸门 `tools/mount_matrix.js` 断言的是带平移的 frame）。
- theirs 的**全部 6 处配置默认值差异**（`gunnerVillagerGunRotation`、`gunnerVillagerIdleGunRotation`、`gunnerVillagerGunOffset`、`gunnerVillagerAimArmPitch`、`renderScale`、`gunMount{ Rifle,Pistol }Offset` 的共同默认 `DEFAULT_MOUNT_OFFSET`）—— **全部是用户实机标定的发布基线，porting theirs 会回退用户的标定**。
- theirs 的 `RenderStateGuard` **主设计**（"保留进入时的 stencil 而不强制 always-pass"）—— 我们的是后续版本（README §5k 的字节码证据 + `forceAlwaysPassStencil` + `selftest_texture_state.js` 第 1 节逐渲染器断言）。
- theirs 的三个渲染器（`ScavRenderer` / `GunnerPillagerGeoRenderer` / `GunnerVillagerRenderer`）**缺少** guard 包裹的形态 —— 我们的是后续工作。
- `GunInHandGeoLayer` 的两处外来绘制护栏（我们多了 `rebindTexture` + `forceAlwaysPassStencil`，README §5w）。
- 朋友的两个脚本原文（硬编码他人 JDK 路径 + 断言他们 guard 的契约，对我们的树会 red）。
- `dev.bat`（朋友的本地环境脚本）、`README.md` 占位符、构建/发布文件的差异（`build.gradle`/`gradle.properties`/`settings.gradle`/`.gitignore` 是"本地诊断版 vs 发布版"）。
- 行尾噪音：`ModelRenderTypes.java`（我们第 25/34 行 CRLF）、`ScavEntity.java`、`gradlew.bat`。

### 需要用户拍板

1. **P3**（`displayContext` 第一人称兜底是否要改，1 行）。
2. **G1 的 A/B**：用户若仍觉得"村民/怪的枪位置不对"，可**临时**在游戏里对比"去掉那一下 `translate(±1/16,0.125,-0.625)`"的效果（等价于 `/tarkovscav client gunpose` 里往前/后挪约 0.64 格做近似），**确认后再决定**是否改 `applyVanillaHandFrame`。**默认建议不改**（会动到用户标定）。
3. **P1 的范围**：是否把 R2/R3/R4/R5 四条恢复全部补上（推荐全补，或至少补 R3/R5 这两条最便宜的）。
4. **`SniperPillagerEntity`/`SniperVillagerEntity`**：是否恢复"`getDefault()` + `onAddedToWorld()`"的防御写法（**先确认我们的发布 jar 没有配置加载时序问题**，有再改）。
5. **可选加固**：`gradle-wrapper.properties` 是否采纳 theirs 的 `distributionSha256Sum` 钉死（与渲染无关）。

### 未核对

- **归属/先后无法证明**：`applyPalmItemFrame`（无平移的手部 frame）与 155 行 `RenderStateGuard` 到底是**朋友自己的改写**还是**我们更早的历史状态**。两种叙事都与证据相容，我无法判定：
  - 支持"朋友改写"：theirs 的注释是对**我们现行设计**的批评（"Repeating that translation moves the grip away from the hand"；"Preserve the incoming stencil test instead of forcing it to always pass"），且我们 README §5f/§5k/§5w 的叙述里**没有**"只有旋转没有平移"或"恢复 12 个 sampler"这两个中间形态。
  - 支持"我们的旧状态"：theirs 整体是更早快照（改名、特性标记全 0）。
  - 尝试过但**不可用**的证据：朋友树 322 个文件的 mtime **全是提取时间** `2026-09-25 06:00`（无区分度）；我们树的 mtime 被批量触碰（`accessTransformer.cfg` 与我们 09-25 22:51 才改过的文件同为"相同"，且存在 09-25 23:25 的 bulk 时间），**无法建立快照时点**。
- **谁更新**：`forge_version`（我们 47.3.0 / theirs 47.4.10）、`geckolib_version`（我们 / theirs 4.8.4）、`tacz_version`（ours / theirs `1.1.8-hotfix`）、`minecraft_version_range`（`[1.20.1,1.21)` vs `[1.20.1]`）。
- **`SniperPillagerEntity`/`SniperVillagerEntity` 属性注册与 config 加载的先后**：未在真实 Forge 生命周期里验证（未运行 gradle/游戏）。
- **"theirs 的 smoke test 会对我们的 guard 失败"是静态推断**（逐条比对断言字段与我们的字段），**未实际编译运行**。
- **README 与代码的一处漂移（我们自己树内）**：README `[client]` 表把 `gunnerVillagerAimArmPitch` 的默认写成 `0.0`，同一行又说"The user's old absolute `-100` is now the offset `-57`"，而 `Config.java` L450 的常量是 `-57.0D` 且 L1776 的 `defineInRange` 就用它 → 表格那一格的 `0.0` 疑似漏改。是否有意，未核对（与朋友无关）。
- **theirs 的配置注释叙述冲突**：theirs 把 `[0,0,-0.7]` 说成 `"Older configs may retain [0, 0, -0.7], a compensation for the previous frame."`，我们 README §5f 把它说成"用户游戏内测量基线"。我只能证明**我们的默认值确实是 `-0.7`**，无法判定哪种历史叙述正确。
- **未逐字读完的大文件**：`Config.java`（两侧 22.8 万 / 28.1 万字节）、`GunBrain.java`、`ModCommands.java`、`sounds.json`、`voice_levels.json`、`lang/*.json`、`*.nbt` 只做了**特性标记 + 抽样 diff + 尺寸/行数**核对（结论：全部由已知后续功能解释），**没有逐行读完**。若需要一个"100% 逐行"的结论，需要单独一轮。

---

### 附：本次分析的中间产物（可复核，均在 `tools/spike/work/friendfix/`）

`map.csv`（322 个文件的状态/大小映射）、`diffclass.csv`（57 个差异文件的行尾归类）、`theirs_*.java` / `ours_*.java`（三个渲染文件的归一化副本）、`diff_GunInHandGeoLayer.txt`、`diff_RenderStateGuard.txt`。
